{
  "name": "@agentic/governance",
  "version": "0.1.0",
  "description": "Governance policy logic: budget thresholds, compliance rules, escalation paths",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx/esm --test 'src/**/*.test.ts'",
    "lint": "tsc --noEmit -p tsconfig.test.json"
  },
  "dependencies": {
    "@agentic/core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.5.0"
  }
}
